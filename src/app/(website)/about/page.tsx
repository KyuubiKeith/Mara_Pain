// === === === === === === == Render == === === === === === ===//

export default function About() {
	return <div>Hello World From The About Page</div>;
}

// === === === === === === == Render == === === === === === ===//
