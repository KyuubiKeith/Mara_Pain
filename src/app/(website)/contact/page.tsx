// === === === === === === == Render == === === === === === ===//

export default function Contact() {
	return <div>Hello World From The Contact Page</div>;
}

// === === === === === === == Render == === === === === === ===//
