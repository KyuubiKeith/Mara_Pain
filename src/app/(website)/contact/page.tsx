// === === === === === === == Imports == === === === === === ===//

// NextJS

// === === === === === === == Imports == === === === === === ===//

//

// === === === === === === == MetaData == === === === === === ===//

// === === === === === === == MetaData == === === === === === ===//

//

// === === === === === === == MetaData == === === === === === ===//

// ==================== Main Render =====================//
export default function Home({children}: any) {
	return <>{children}</>;
}

// ==================== Main Render =====================//

// === === === === === === == MetaData == === === === === === ===//

//

// ==================== References =====================//

// ==================== References =====================//
