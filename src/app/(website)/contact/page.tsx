// === === === === === === == Imports == === === === === === ===//

// Components
// import ThemeToggle from '@/lib/components/molecules/themeToggle';

// === === === === === === == Imports == === === === === === ===//

//

// === === === === === === == MetaData == === === === === === ===//

// === === === === === === == MetaData == === === === === === ===//

//

// === === === === === === == Render  == === === === === === ===//

export default function Contact() {
	return <>Hello World Contact</>;
}
// === === === === === === == Render == === === === === === ===//
