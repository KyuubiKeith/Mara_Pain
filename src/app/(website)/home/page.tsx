// === === === === === === == Render == === === === === === ===//

export default function Home() {
	return (
		<>
			<p>
				<i>Tip</i> Abstract all the things! <br />
				<i>Tip</i> Never forget to save! <br />
			</p>
		</>
	);
}

// === === === === === === == Render == === === === === === ===//
