// === === === === === === == Render == === === === === === ===//

export default function Home() {
	return <div>Hello World From The Home Page</div>;
}

// === === === === === === == Render == === === === === === ===//
