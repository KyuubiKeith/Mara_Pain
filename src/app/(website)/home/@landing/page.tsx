// === === === === === === == Render == === === === === === ===//

export default function Landing() {
	return (
		<div>
			Hello World From <strong>The Landing Page</strong>{' '}
		</div>
	);
}

// === === === === === === == Render == === === === === === ===//
