// === === === === === === == Render == === === === === === ===//

export default function Landing() {
	return <div>Hello World From The Landing Page</div>;
}

// === === === === === === == Render == === === === === === ===//
