// === === === === === === == Render == === === === === === ===//

export default function WorkSpace() {
	return (
		<div>
			Hello World From <strong>The WorkSpace Page</strong>{' '}
		</div>
	);
}

// === === === === === === == Render == === === === === === ===//
