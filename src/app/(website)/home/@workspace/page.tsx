// === === === === === === == Render == === === === === === ===//

export default function WorkSpace() {
	return <div>Hello World From The WOrkSpace Page</div>;
}

// === === === === === === == Render == === === === === === ===//
