// === === === === === === == Render == === === === === === ===//

export default function Home({children}: any) {
	return <>{children}</>;
}

// === === === === === === == Render == === === === === === ===//
